Optional custom alert sounds

Drop audio files here to override generated tones.

Default files now used by HWOS:
- alert.wav — used for every NWS/weather alert by default
- admin.wav — used for administrative messages by default

If a file is missing or cannot play, the browser falls back to the built-in generated tone.
Supported formats are wav, mp3, and ogg.
